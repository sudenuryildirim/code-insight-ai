// Swapped in for production builds (`ng build`) via angular.json's fileReplacements.
// Fill this in with the deployed backend's real URL before building for production.
export const environment = {
  production: true,
  apiBaseUrl: 'https://REPLACE-WITH-YOUR-BACKEND-URL',
};
